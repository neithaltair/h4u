bundle.js = contiene la información que vamos a consultar con el programa. 
controlc.sh = script para indicar como se comportará el sistema cuando se le cancele un proceso con control + c.
search_machine.sh = script completo con el que se realizarán las consultas de las máquinas. 
